30px pill for counters, filters and statuses. The app top bar uses `dark` chips for XP and streak; schedule rows use `dark` chips with a coloured `dot`.

```jsx
<Chip tone="dark">1,230 XP</Chip>
<Chip tone="dark" dot="var(--status-event)">Event</Chip>
<Chip tone="soft">Networking</Chip>
```