Big display headline closing with the signature orange full stop. One per screen region.

```jsx
<SectionHeading>Selected projects</SectionHeading>
<SectionHeading size={56} accent="brands">Design that builds</SectionHeading>
```

Sentence case always. `period={false}` when the headline is a question.