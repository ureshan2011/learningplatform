Typographic ICTCAMPUS lockup — the brand has no logo file yet, so this is the mark everywhere.

```jsx
<Wordmark size={26}/>
<Wordmark onDark mono/>
```

Replace this component's internals (one place) when real logo SVGs arrive.