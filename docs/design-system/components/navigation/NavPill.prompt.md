Nav items inside the floating near-black pill bar on marketing pages. Active item becomes an orange pill.

```jsx
<NavPill items={['Home','Programmes','Campus','Contact']} active={tab} onSelect={setTab}/>
```