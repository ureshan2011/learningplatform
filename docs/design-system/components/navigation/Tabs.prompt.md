Segmented pill tabs for switching views inside a panel.

```jsx
<Tabs items={['Overview','Modules','Results']} active={t} onSelect={setT}/>
<Tabs onDark items={['Today','Week']} active={t} onSelect={setT}/>
```