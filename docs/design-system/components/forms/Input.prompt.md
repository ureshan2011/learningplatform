44px labelled text field for forms (enrolment, login, settings).

```jsx
<Input label="Email" placeholder="you@example.com"/>
<Input label="Student ID" error="That ID doesn't match our records."/>
<Input onDark label="Display name"/>
```