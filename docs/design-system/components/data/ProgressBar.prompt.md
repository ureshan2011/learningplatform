6px pill progress track with orange fill; the module header's key element.

```jsx
<ProgressBar value={75} onDark/>
<ProgressBar value={40} showLabel={false}/>
```

Fill animates once, 340ms, ease-out. Never loop it.